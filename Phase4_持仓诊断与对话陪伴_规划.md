# 方案 B · Phase 4 规划 —— 持仓诊断（M2）+ 对话陪伴（M4）

> 状态：**调研方案，待你确认后实现**。本文件不写最终代码，只定路线、指标、边界。
> 项目根目录：`D:\Agent-wyf\fund-daily\`
> 前置：Phase 0/1/1.5/2 已完成（数据层 akshare + 纯 pandas 规则引擎 + 每日日报含盘中估值与红绿灯/止盈/异动信号）。

---

## 1. Phase 4 要补的是什么

回到你最初的两个诉求：
1. 何时买/卖 + 持仓建议
2. 每天跟踪实时大盘、给当天数据和分析

前几期已交付：
- M1 每日跟踪（净值 + 盘中估值）✅
- M3 买卖信号（红绿灯/止盈/异动）✅

**Phase 4 = 补齐 M2 + M4**：

| 模块 | 你要的 | 现状 | Phase 4 交付 |
|---|---|---|---|
| **M2 持仓诊断** | 持仓结构/占比是否合理、是不是假分散 | ⚪ 0% | 集中度、相关性、风险、再平衡建议 |
| **M4 小白陪伴** | 说人话的解读/问答/科普 | 🟡 被动 | 周/月体检报告 + 随时用大白话问我 |

一句话：Phase 2 告诉你"某只该注意什么"，Phase 4 告诉你"**整体组合健不健康、要不要调结构**"，并让你能**用自然语言问它**。

---

## 2. 可行性探针结论（已用真实数据验证）

我对你已缓存的 12 只基金 NAV 历史做了只读计算（脚本 `src/phase4_probe.py`），确认诊断功能**有真实数据支撑**：

### 2.1 分类占比（按持仓市值）
```
半导体/芯片   22.5%   ████████████
碳中和/制造   20.2%   ███████████
黄金          17.3%   █████████
新能源/汽车   16.3%   ████████
纳斯达克/QDII 11.8%   ███████
电网/电力      4.2%
债券/避险      2.3%
科技/数字经济  1.9%
机器人/高端    1.9%
资源/周期      1.5%
```
- **前 3 只（芯片+碳中和+新能源）= 59%**，且同属"成长/周期"风险桶。
- 黄金 17.3% + QDII 11.8% + 债券 2.3% = 31.4% 提供一定对冲。

### 2.2 集中度
- 单只基金 HHI = **0.1606**（刚好越过 0.15 的"较分散"线 → 已偏中等集中）
- 按分类 HHI = **0.1654**
- 含义：从"数量"看 12 只挺多，但从"风险敞口"看已不够分散。

### 2.3 相关性（抓出"假分散"）
平均两两日收益率相关 **0.312**（整体不算极高，因黄金/债券/QDII 拉低），但**局部高度同质**：
```
+0.867  碳中和  ↔  机器人     （几乎同涨同跌！）
+0.736  芯片指增 ↔  数字经济
+0.717  数字经济 ↔  电网设备
+0.693  新能源   ↔  资源优选
+0.673  易方达全球 ↔ 数字经济
```
**关键洞察**：你"分散"买了很多只，但碳中和+机器人、芯片+数字经济 这两对几乎一起动——**数量分散 ≠ 风险分散**。这正是 M2 要揭示的。

### 2.4 组合风险（等权近似）
- 年化波动率 ≈ **20.0%**
- 样本期最大回撤 ≈ **-14.8%**

> 结论：集中度、相关性、风险三类指标全部可计算且结果有意义。Phase 4 不是空谈，能直接给你"该注意什么"。

---

## 3. 实现路线对比与选型

### 3.1 M2 持仓诊断（规则计算部分）
| 路线 | 说明 | 取舍 |
|---|---|---|
| **A. 本地 pandas 规则（推荐）** | 复用现有栈，HHI/相关矩阵/波动/回撤都是 `pandas` 几行 | ✅ 确定性、可复现、零新依赖，与 Phase 2 一致 |
| B. 量化框架（Qlib/PyPortfolio） | 专业组合优化 | ❌ 杀鸡用牛刀，且要分钟级数据，你的日频净值喂不进 |
| C. LLM 直接算 | 把数据丢给我让我说集中度 | ❌ 不 deterministic，每次结论漂移，无法复查 |

**→ 选 A**：新建 `src/diagnostics.py`，纯函数输出结构化健康字典。

### 3.2 M4 对话陪伴（交互部分）
| 路线 | 说明 | 取舍 |
|---|---|---|
| **A. WorkBuddy 当伴侣（推荐）** | 你用大白话问我，我读 `portfolio.json`+缓存+`diagnostics` 输出回答 | ✅ 零新代码、最自然、我本就是 LLM；诊断模块让我的回答有数据底座 |
| B. 独立 CLI 问答脚本 | `src/ask.py` 读数据 + 调 LLM API 回答 | ⚠️ 多一个维护面，且本应用里我已是更好的"伴侣"；仅当你要**无界面/定时跑**才有价值 |
| C. RAG + 向量库 | 把基金文档向量化再检索 | ❌ 12 只基金根本不需要检索，纯粹增负担 |

**→ 推荐 A 为主**：诊断模块喂给我的回答；同时**顺手做一个 `ask.py` 薄封装**（可选，你定），让你能在命令行 `python ask.py "我的组合风险高吗"` 直接拿结果，底层仍调 LLM。

### 3.3 周/月体检报告
- 复用现有 `phase1_daily_report.py` 的 HTML 引擎，新增 `report_weekly.html` / `report_monthly.html`。
- 在日报基础上叠加 **M2 诊断区块**（集中度卡 / 相关性热点 / 风险指标 / 再平衡建议）。
- **不新增框架**，只是报告模板的扩展。

---

## 4. 诊断指标设计（M2）

`diagnostics.py` 输出以下结构化指标（阈值写进 `portfolio.json` 的 `diagnostics` 块，你随时能改）：

| 指标 | 算法 | 预警线（默认） | 输出形态 |
|---|---|---|---|
| 分类占比 | 各 category 市值 / 总市值 | 单一风险桶 >40% 预警 | 占比条形 |
| 单只占比 | 单基金市值 / 总市值 | 单只 >25% 预警 | 列表 |
| HHI 集中度 | Σ权重² | >0.15 偏集中 | 数值+评级 |
| 相关性热点 | 两两日收益相关矩阵 | 相关系数 >0.7 标"同质化" | 热点对列表 |
| 组合波动率 | 日收益 std ×√252 | 对比偏股基金基准 ~20% | 数值 |
| 最大回撤 | 累计收益峰谷差 | 样本期内 | 数值 |
| 再平衡建议 | 基于占比/相关性/预警 | 规则生成 | 白话建议 |

**再平衡建议的逻辑示例**（规则，非预测）：
- 若"芯片+碳中和+新能源"合计 >50% 且彼此高相关 → 提示"成长/周期敞口过重，可考虑将小部分转入黄金/债券/宽基以降相关"。
- 若某对相关性 >0.85 → 提示"X 与 Y 高度同质，保留一只即可，另一只是重复风险"。
- 若 HHI >0.15 → 提示"整体偏集中，定投新增资金优先流向低占比/低相关类别"。

---

## 5. 对话陪伴形态（M4）

**主形态：你直接问我，我读数据答。**
你以后可以这样说（我都能接）：
- "我的组合现在风险高吗？"
- "碳中和和机器人是不是白买两只了？"
- "我定投的节奏合理吗？新增的钱该加哪？"
- "最近回撤多少了？要不要慌？"

我的回答会基于 `diagnostics.py` 的实时输出，**说人话、带数据、不做预测、不自动交易**。

**可选 `ask.py` 封装**：命令行直接问，适合你以后想脱离对话界面跑（如接定时任务生成问答摘要）。实现极薄：加载 portfolio+diagnostics → 拼成上下文 → 调 LLM。是否做由你定。

---

## 6. 周/月体检报告形态

在现有日报顶部加"**组合体检**"区块：
```
┌─ 组合体检（截至 2026-07-20） ─────────────┐
│ 风险评级：中等偏高（成长/周期占 59%）      │
│ 集中度 HHI：0.16（偏集中）                 │
│ 年化波动：~20% ｜ 最大回撤：-14.8%         │
│ 假分散警报：碳中和↔机器人 0.87 同质        │
│ 再平衡建议：……（白话）                    │
└──────────────────────────────────────────┘
```
- 周报：每周末生成；月报：每月末生成。
- 复用 `data/history/` 缓存，增量计算。

---

## 7. 阈值与边界

- 所有阈值写入 `portfolio.json` 的 `diagnostics` 块（默认：风险桶>40%、单只>25%、HHI>0.15、相关>0.7）。
- **边界（必须说清）**：
  - ❌ 不预测未来涨跌，不推荐"必买/必卖"具体时点。
  - ❌ 相关性基于**历史**日收益，不代表未来必然同动（但高相关是强信号）。
  - ❌ 再平衡建议是**规则提示**，不是指令；最终拍板是你。
  - ❌ 不自动交易（支付宝无接口，一贯如此）。
  - ⚠️ 波动率/回撤为等权近似，未考虑你定投的现金流时序（精确需成本数据，见下方待确认）。

---

## 8. 实施步骤（确认后执行）

1. 新建 `src/diagnostics.py`：实现 §4 全部指标，纯函数，单测可用探针数据验证。
2. `portfolio.json` 增加 `diagnostics` 配置块（阈值）。
3. 改造报告引擎：新增 `report_weekly.html` / `report_monthly.html`，含组合体检区块。
4. （可选）`src/ask.py` 薄封装，支持命令行问答。
5. 实跑你的 12 只，生成首份**周/月体检报告**给你看。
6. 记忆记录 + 交付文档。

---

## 9. 待你确认的点

**Q1：M4 对话陪伴形态**
- A（推荐）：我就当伴侣，你直接问我，我读诊断数据答（零新代码）。
- A+B：再加一个 `ask.py` 命令行封装（可选，方便以后定时跑）。
- 你选哪个？

**Q2：周/月体检报告**
- 两个都做（周报 + 月报）？还是只做月报？
- （日报已是每日，周/月报是"拉远看结构"的补充。）

**Q3：阈值接受默认吗？**
- 风险桶>40% / 单只>25% / HHI>0.15 / 相关>0.7，行不行？还是要调？

**Q4（可选，提升精度）**：若想让波动率/回撤更准（考虑你定投现金流），可补**每只累计投入金额**，我据此做资金加权风险指标。不补也能跑（等权近似）。

> 你回复这几点，我就开工 Phase 4。在此之前不会写实现代码。
