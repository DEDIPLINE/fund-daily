# M5：消息面 / 舆情驱动的持仓操作建议 — 方案

> 新增需求：根据新闻源、大V 等消息来源，针对 12 只持仓给出操作参考。
> 文档状态：待确认方案（先分析、后动手）。

## 一、先说两个绕不开的现实约束

### 1. "大V"的真实可获取性
- 真正"带 ID 的具体大V"（微博博主、雪球大V、微信公众号）内容都**封闭在平台内**，需要登录 cookie / 授权，且大多违反平台 ToS、极易被封。
- akshare 能稳定拿到的，是**聚合后的讨论情绪与热帖**：雪球热帖（`stock_hot_tweet_xq`）、微博舆情（`stock_js_weibo_report`）、股吧/热度榜（`stock_hot_rank_em`）。这代表"大V + 散户的整体情绪温度计"，**不是某个人原话**。
- 想拿到"具体某大V原话"，只有一条合规路：**你手动把帖子/文章复制进项目**，由助手解析点评（见方案 C）。

### 2. 合规与定位（必做）
- 本模块定位为 **"消息面信息聚合 + 参考"**，**不是投资建议**，不输出"买入/卖出"指令。
- 大V情绪噪声大、易滞后、常有利益倾向；新闻可能滞后或被操纵。输出必须：标注来源+日期、标注利好/利空/中性、对大V情绪加"噪声警告"、底部固定免责声明。

## 二、三个候选方案

| 维度 | 方案 A：akshare 全自动聚合 | 方案 B：外部新闻/舆情 API | 方案 C：用户手动投递大V |
|---|---|---|---|
| 数据源 | akshare 新闻+研报+雪球热帖+微博舆情+热度榜 | 财联社/同花顺/聚合数据/RSS 等 | 用户粘贴大V帖子/链接/截图文字 |
| 大V覆盖 | 聚合情绪代理（非原话） | 取决于 API | 真正原话 |
| 依赖 | 仅 akshare（已有） | 需申请 key，部分付费 | 无 |
| 实时性 | 盘后/每日可跑 | 取决于 API | 看你什么时候贴 |
| 合规风险 | 低 | 中（key 管理） | 最低 |
| 工作量 | 中（建 theme_map + 抓取+LLM汇总） | 高（接 API+维护） | 低 |
| 缺点 | 大V是统计情绪；部分接口偶有限流 | 要 key/钱；架构改动大 | 非自动，需你动手 |

## 三、推荐：A + C 混合

- **自动化 backbone = 方案 A**：每天/每周跑，给 12 只持仓各自生成一张"消息面参考卡"（相关新闻 + 研报观点 + 雪球/微博情绪 + 热度变化 + 利好利空标注）。
- **定向补充 = 方案 C**：你刷到特别在意的大V观点，复制进 `inbox/`，助手归类到对应持仓并点评，和自动新闻一起看。
- 全程不输出买卖指令，只做"信息参考 + 情绪温度计 + 风险提示"。

### 落地设计（若采用）
1. `portfolio.json` 增加 `theme_map`：每只基金 → 行业/概念板块关键词（如 014194芯片→半导体/芯片概念；000218黄金→有色金属/贵金属；270042纳指→宏观+纳指；008173债券→利率/货币政策）。
2. 新模块 `src/news_signal.py`：
   - 按 theme_map 抓 `stock_news_em` / `stock_research_report_em` / `stock_hot_tweet_xq` / `stock_js_weibo_report` / `news_economic_baidu` / `fund_announcement_report_em`
   - LLM（即本助手）按持仓汇总成"利好/利空/中性 + 来源 + 日期 + 噪声警告"
   - 输出 `data/reports/news_YYYYMMDD.html`（消息面参考卡）
3. 频率：建议**每周一张卡**（与 phase4 周报合并），盘后自动；手动投递随时触发。
4. 复用现有 phase4 的 `ask.py` 对话入口，可问"今天半导体有什么消息"。

## 四、需要你拍板的问题

1. **方案选择**：A 全自动 / C 手动 / A+C 混合（我推荐 A+C）？
2. **频率**：每天盘后？还是每周一张卡？
3. **大V口径**：接受"聚合情绪代理"（雪球/微博热帖）？还是你更想要"手动贴具体大V原话"为主？
4. **输出形态**：独立"消息面参考卡" HTML？还是并入现有每日日报 / 周报？
